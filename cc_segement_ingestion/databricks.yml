# This is a Databricks asset bundle definition for cc_segement_ingestion.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: cc_segement_ingestion
  uuid: 5eecea47-d542-49cb-ae62-8f5edcb8bbe6

include:
  - resources/*.yml
  - resources/*/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-a1e27fcd-fa88.cloud.databricks.com
    variables:
      catalog: workspace
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://dbc-a1e27fcd-fa88.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/kl1147@georgetown.edu to make sure we only have a single copy.
      root_path: /Workspace/Users/kl1147@georgetown.edu/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: workspace
      schema: prod
    permissions:
      - user_name: kl1147@georgetown.edu
        level: CAN_MANAGE
